# kakaopay proposal-template

## 카카오페이 제안에 반드시 포함할 것

1. `approval_url`과 `approve` 책임 분리
2. `tid` 중심 저장 모델
3. 리다이렉트 도착만으로 완료 처리하지 않는 원칙
4. 승인/취소 중복 방지 전략
5. 도메인 등록과 플랫폼별 복귀 URL 기준
6. 조회 API를 쓰는 후속 보강 전략
